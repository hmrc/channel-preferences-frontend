# This is the default configuration file for the application.
# Values are overriden using system properties on a per-environment basis.
# ~~~~~

# Session Timeout
# ~~~~
# The default session timeout for the app is 15 minutes (900seconds).
# Updating this is the responsibility of the app - it must issue a new cookie with each request or the session will
# timeout 15 minutes after login (regardless of user activity).
session.maxAge=900

# Secret key
# ~~~~~
# The secret key is used to secure cryptographics functions.
# If you deploy your application to several instances be sure to use the same key!
application.secret="yNhI04vHs9<_HWbC`]20u`37=NGLGYY5:0Tg5?y`W<NoJnXWqmjcgZBec@rOxb^G"

# Base 64 encoded MD5 hash of application.secret
cookie.encryption.key="gvBoGdgzqG1AarzF1LY0zQ=="

sso.encryption.key="P5xsJ9Nt+quxGZzB4DeLfw=="

# Session configuration
# ~~~~~
application.session.httpOnly=false

application.session.secure=false

# HTTP Response headers control
# ~~~~~
http.cacheControl=0


# The application languages
# ~~~~~
application.langs="en"

# Global object class
# ~~~~~
# Define the Global object class for this application.
# Default to Global in the root package.
application.global=config.Global

# Router 
# ~~~~~
# Define the Router object to use for this application.
# This router will be looked up first when the application is starting up,
# so make sure this is the entry point. 
# Furthermore, it's assumed your route file is named properly. 
# So for an application router like `my.application.Router`,
# you may need to define a router file `conf/my.application.routes`.
# Default to Routes in the root package (and conf/routes)
# application.router=my.application.Routes


# Evolutions
# ~~~~~
# You can disable evolutions if needed
# evolutionplugin=disabled

# Logger
# ~~~~~
# You can also configure logback (http://logback.qos.ch/), by providing a logger.xml file in the conf directory .

# Root logger:
logger.root=ERROR

# Logger used by the framework:
logger.play=INFO

# Logger provided to your application:
logger.application=DEBUG

# Metrics plugin settings - graphite reporting is configured on a per env basis
metrics {
    name = govuk-tax
    rateUnit = SECONDS
    durationUnit = SECONDS
    showSamples = true
    jvm = true
    enabled = true
}

govuk-tax {

    Dev {
        metrics {
            graphite {
                host = graphite
                port = 2003
                prefix = play.tax.
                enabled = false
            }
        }

        externalLinks {
            servicesUrl="https://secure.hmce.gov.uk"
            businessTax.manageTaxes.servicesHome=${govuk-tax.Dev.externalLinks.servicesUrl}"/ecom/login/index.html"
            customsUrl="https://customs.hmrc.gov.uk"
            businessTax.manageTaxes.ncts=${govuk-tax.Dev.externalLinks.customsUrl}"/nctsPortalWebApp/ncts.portal?_nfpb=true&pageLabel=httpssIPageOnlineServicesAppNCTS_Home"
            businessTax.manageTaxes.rcsl=${govuk-tax.Dev.externalLinks.customsUrl}"/rcsl/httpssl/Home.do"
            businessTax.manageTaxes.ecsl=${govuk-tax.Dev.externalLinks.customsUrl}"/ecsl/httpssl/start.do"
        }

        portal {
            url="http://localhost:8080/portal"
            ssoUrl=${govuk-tax.Dev.portal.url}"/ssoin"
            destinationRoot=${govuk-tax.Dev.portal.url}
            domainWhiteList=[
                localhost
            ]
            loggedOutUrl=${govuk-tax.Dev.portal.url}"/loggedout"
            ssoInLogoutUrl=${govuk-tax.Dev.portal.url}"/ssoin/logout?host=http://localhost:9000"
            destinationPath.home=${govuk-tax.Dev.portal.url}"/home"
            destinationPath.epayeAccountDetails=${govuk-tax.Dev.portal.url}"/paye/org/<empref>/account"
            destinationPath.saAccountDetails=${govuk-tax.Dev.portal.url}"/self-assessment/<affinitygroup>/<utr>/account"
            destinationPath.saFileAReturn=${govuk-tax.Dev.portal.url}"/self-assessment-file/<year>/<affinitygroup>/<utr>/return"
            destinationPath.vatAccountDetails=${govuk-tax.Dev.portal.url}"/vat/trader/<vrn>/account"
            destinationPath.vatFileAReturn=${govuk-tax.Dev.portal.url}"/vat-file/trader/<vrn>/return"
            destinationPath.vatHelpDesk=${govuk-tax.Dev.portal.url}"/home"
            destinationPath.vatOnlineAccount=${govuk-tax.Dev.portal.url}"/vat/trader/<vrn>/directdebit"
            destinationPath.ctAccountDetails=${govuk-tax.Dev.portal.url}"/corporation-tax/org/<ctutr>/account/balanceperiods"
            destinationPath.btDirectDebits=${govuk-tax.Dev.portal.url}"/directdebits/welcome"
            destinationPath.ctFileAReturn=${govuk-tax.Dev.portal.url}"/corporation-tax/org/<ctutr>"
            destinationPath.otherServicesEnrolment=${govuk-tax.Dev.portal.url}"/home/services/enroll"
            destinationPath.otherServices=${govuk-tax.Dev.portal.url}"/other-services"
            destinationPath.servicesDeEnrolment=${govuk-tax.Dev.portal.url}"/home/removeservices"
            destinationPath.businessRegistration=${govuk-tax.Dev.portal.url}"/business-registration/introduction"
            destinationPath.manageTaxes.servicesHome=${govuk-tax.Dev.portal.url}"/home/services"
            destinationPath.manageTaxes.emcs=${govuk-tax.Dev.portal.url}"/emcs"
            destinationPath.manageTaxes.ics=${govuk-tax.Dev.portal.url}"/ics"
            destinationPath.manageTaxes.machinegames=${govuk-tax.Dev.portal.url}"/machine-games-duty"
            destinationPath.manageTaxes.nova=${govuk-tax.Dev.portal.url}"/nova"
            destinationPath.manageTaxes.euvat=${govuk-tax.Dev.portal.url}"/eu-vat"
            #destinationPath.manageTaxes.ncts=${govuk-tax.Dev.portal.url}"/nctsPortalWebApp/ncts.portal?_nfpb=true&pageLabel=httpssIPageOnlineServicesAppNCTS_Home"
            #destinationPath.manageTaxes.ecsl=${govuk-tax.Dev.portal.url}"/ecsl/httpssl/start.do"
            #destinationPath.manageTaxes.rcsl=${govuk-tax.Dev.portal.url}"/rcsl/httpsslHome.do"
        }

        platform {
            frontend {
                protocol=http
                host=localhost
                port=9000
            }
        }

        services {
            timeout = 5 seconds

            delay-response = 2 seconds

            protocol=http

            auth {
                host=localhost
                port=8500
            }

            ct {
                host=localhost
                port=8647
            }

            datastream {
                host=localhost
                port=8100
                enabled=true
                traceRequests=true
            }

            government-gateway {
                host=localhost
                port=8080
            }

            keystore {
                host=localhost
                port=8400
            }

            paye {
                host=localhost
                port=8600
            }

            matching {
                host=localhost
                port=8520
            }

            saml {
                host=localhost
                port=8080
            }

            sa {
                host=localhost
                port=8900
            }

            txqueue {
                host=localhost
                port=8700
            }

            vat {
                host=localhost
                port=8880
            }

            epaye {
                 host=localhost
                 port=8090
            }

            preferences {
                host=localhost
                port=8025
            }

            email {
                host=localhost
                port=8300
            }
        }
    }
    Prod {
        metrics {
            graphite {
                host = graphite
                port = 2003
                prefix = play.tax.
                enabled = true
            }
        }

        externalLinks {
            servicesUrl="https://secure.hmce.gov.uk"
            businessTax.manageTaxes.servicesHome=${govuk-tax.Prod.externalLinks.servicesUrl}"/ecom/login/index.html"
            customsUrl="https://customs.hmrc.gov.uk"
            businessTax.manageTaxes.ncts=${govuk-tax.Prod.externalLinks.customsUrl}"/nctsPortalWebApp/ncts.portal?_nfpb=true&pageLabel=httpssIPageOnlineServicesAppNCTS_Home"
            businessTax.manageTaxes.rcsl=${govuk-tax.Prod.externalLinks.customsUrl}"/rcsl/httpssl/Home.do"
            businessTax.manageTaxes.ecsl=${govuk-tax.Prod.externalLinks.customsUrl}"/ecsl/httpssl/start.do"
        }

        portal {
            url="http://localhost:8080/portal"
            ssoUrl=${govuk-tax.Prod.portal.url}"/ssoin"
            destinationRoot=${govuk-tax.Prod.portal.url}
            domainWhiteList=[
                localhost
            ]
            loggedOutUrl=${govuk-tax.Prod.portal.url}"/loggedout"
            ssoInLogoutUrl=${govuk-tax.Prod.portal.url}"/ssoin/logout?host=http://localhost:9000"
            destinationPath.home=${govuk-tax.Prod.portal.url}"/home"
            destinationPath.epayeAccountDetails=${govuk-tax.Prod.portal.url}"/paye/org/<empref>/account"
            destinationPath.saAccountDetails=${govuk-tax.Prod.portal.url}"/self-assessment/<affinitygroup>/<utr>/account"
            destinationPath.saFileAReturn=${govuk-tax.Prod.portal.url}"/self-assessment-file/<year>/<affinitygroup>/<utr>/return"
            destinationPath.vatAccountDetails=${govuk-tax.Prod.portal.url}"/vat/trader/<vrn>/account"
            destinationPath.vatFileAReturn=${govuk-tax.Prod.portal.url}"/vat-file/trader/<vrn>/return"
            destinationPath.vatHelpDesk=${govuk-tax.Prod.portal.url}"/home"
            destinationPath.vatOnlineAccount=${govuk-tax.Prod.portal.url}"/vat/trader/<vrn>/directdebit"
            destinationPath.ctAccountDetails=${govuk-tax.Prod.portal.url}"/corporation-tax/org/<ctutr>/account/balanceperiods"
            destinationPath.btDirectDebits=${govuk-tax.Prod.portal.url}"/directdebits/welcome"
            destinationPath.ctFileAReturn=${govuk-tax.Prod.portal.url}"/corporation-tax/org/<ctutr>"
            destinationPath.otherServicesEnrolment=${govuk-tax.Prod.portal.url}"/home/services/enroll"
            destinationPath.otherServices=${govuk-tax.Prod.portal.url}"/other-services"
            destinationPath.servicesDeEnrolment=${govuk-tax.Prod.portal.url}"/home/removeservices"
            destinationPath.businessRegistration=${govuk-tax.Prod.portal.url}"/business-registration/introduction"
            destinationPath.manageTaxes.servicesHome=${govuk-tax.Prod.portal.url}"/home/services"
            destinationPath.manageTaxes.emcs=${govuk-tax.Prod.portal.url}"/emcs"
            destinationPath.manageTaxes.ics=${govuk-tax.Prod.portal.url}"/ics"
            destinationPath.manageTaxes.machinegames=${govuk-tax.Prod.portal.url}"/machine-games-duty"
            destinationPath.manageTaxes.nova=${govuk-tax.Prod.portal.url}"/nova"
            destinationPath.manageTaxes.euvat=${govuk-tax.Prod.portal.url}"/eu-vat"
            #destinationPath.manageTaxes.ncts=${govuk-tax.Prod.portal.url}"/nctsPortalWebApp/ncts.portal?_nfpb=true&pageLabel=httpssIPageOnlineServicesAppNCTS_Home"
            #destinationPath.manageTaxes.ecsl=${govuk-tax.Prod.portal.url}"/ecsl/httpssl/start.do"
            #destinationPath.manageTaxes.rcsl=${govuk-tax.Prod.portal.url}"/rcsl/httpsslHome.do"
        }

        platform {
            frontend {
                protocol=http
                host=localhost
                port=9000
            }
        }

        services {
            timeout = 30 seconds

            auth {
                host=auth.service
                port=80
            }

            ct {
                host=ct.service
                port=80
            }

            datastream {
                host=datastream.service
                port=80
                enabled=true
                traceRequests=true
            }

            keystore {
                host=key-store.service
                port=80
            }

            government-gateway {
                host=government-gateway.service
                port=80
            }

            matching {
                host=matching.service
                port=80
            }

            saml {
                host=saml.service
                port=80
            }

            sa {
                host=sa.service
                port=80
            }

            paye {
                host=paye.service
                port=80
            }

            txqueue {
                host=txqueue.service
                port=80
            }

            vat {
                host=vat.service
                port=80
            }

            epaye {
                 host=epaye.service
                 port=80
            }

            preferences {
                host=preferences.service
                port=80
            }

            email {
                host=email.service
                port=80
            }
       }
    }
}
